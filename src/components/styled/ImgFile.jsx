import styled from "styled-components";

export const ImgFile = styled("div")(({ theme }) => ({
  border: "1px dashed #707070",
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
}));

export default ImgFile;
