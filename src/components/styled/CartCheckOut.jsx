import styled from "styled-components";

export const CartCheckOut = styled("div")(({ theme }) => ({
  display: "flex",
  justifyContent: "center",
  borderRadius: "10px",
  width: "324px",
  marginTop: "25px",
}));

export default CartCheckOut;
